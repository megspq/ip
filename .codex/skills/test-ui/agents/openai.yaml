interface:
  display_name: "Test UI"
  short_description: "Run console UI tests from the project test plan"
  default_prompt: "Run the console UI tests recorded in test/ui-test-plan.md and show the full test transcript."
